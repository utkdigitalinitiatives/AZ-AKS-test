# Missing: clusters/aks-cluster/apps.yaml  
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: infrastructure
  path: ./clusters/aks-cluster/apps
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system  # This references the AKS-managed GitRepository